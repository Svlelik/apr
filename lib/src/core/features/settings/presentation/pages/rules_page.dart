import 'package:flutter/material.dart';

class SettingsRulesPage extends StatelessWidget {
  const SettingsRulesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SettingsRulesPage'),
    );
  }
}
